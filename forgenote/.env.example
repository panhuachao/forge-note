# 环境变量示例（复制为 .env 后填写真实值，.env 已被 gitignore 忽略，请勿提交）
# 使用方式：npm run package:mac 会通过 dotenv -e .env 自动加载本文件后再启动 electron-builder

# GitHub Personal Access Token（classic），用于 electron-builder 发布到 GitHub Releases。
# 需要 repo 权限（私有仓库发布必须）。以 ghp_ 开头。
# 创建地址：GitHub → Settings → Developer settings → Personal access tokens (classic)
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：指定发布目标仓库（默认读取 package.json 中 build.publish 的 owner/repo）
# GH_OWNER=forgenote
# GH_REPO=forgenote
